<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EDITOR</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/dashboard.css" rel="stylesheet">
</head>
<body>
  <!-- ASIDE MENU LATERAL -->
  <%- include('partials/aside') %>
  <!-- NAVBAR SUPERIOR -->
  <%- include('partials/nav') %>
  <div class="main-content">
    <main class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
      <section class="col-12 col-md-6 col-lg-4">
        <form action="/update/<%= pelicula.id %>" method="POST" autocomplete="off">
          <div class="card">
            <div class="card-header text-center"><strong>Editar Película</strong></div>
            <div class="card-body">
              <div class="mb-3 form-floating">
                <input type="text" class="form-control" id="titulo" name="titulo" placeholder="Título de la Película" value="<%= pelicula.titulo %>" required>
                <label for="titulo">Título de la Película</label>
              </div>
              <div class="mb-3 form-floating">
                <input type="text" class="form-control" id="autor" name="autor" placeholder="Autor" value="<%= pelicula.autor %>" required>
                <label for="autor">Autor</label>
              </div>
              <div class="mb-3 form-floating">
                <input type="number" class="form-control text-center" value="<%= pelicula.duracionmin %>" min="30" max="240" step="15" id="duracion" name="duracion" placeholder="Duración en Minutos" required>
                <label for="duracion">Duración en Minutos</label>
              </div>
              <div class="mb-3 form-floating">
                <input type="text" class="form-control text-center" pattern="[0-9]+" minlength="4" maxlength="4" id="fecha_estreno" name="fecha_estreno" placeholder="Fecha de Estreno" value="<%= pelicula.estreno %>" required>
                <label for="fecha_estreno">Fecha de Estreno</label>
              </div>
              <div class="mb-3 form-floating">
                <select class="form-select" id="puntuacion" name="puntuacion" required>
                  <option value="" disabled>Selecciona puntuación</option>
                  <option value="1" <%= pelicula.puntuacion == 1 ? 'selected' : '' %>>★☆☆☆☆</option>
                  <option value="2" <%= pelicula.puntuacion == 2 ? 'selected' : '' %>>★★☆☆☆</option>
                  <option value="3" <%= pelicula.puntuacion == 3 ? 'selected' : '' %>>★★★☆☆</option>
                  <option value="4" <%= pelicula.puntuacion == 4 ? 'selected' : '' %>>★★★★☆</option>
                  <option value="5" <%= pelicula.puntuacion == 5 ? 'selected' : '' %>>★★★★★</option>
                </select>
                <label for="puntuacion">Puntuación</label>
              </div>
            </div>
            <div class="card-footer text-end">
              <button type="submit" class="btn btn-primary btn-sm">Actualizar</button>
              <a href="/mant_peliculas" class="btn btn-secondary btn-sm">Cancelar</a>
            </div>
          </div>
        </form>
      </section>
    </main>
  </div>
  <!-- FOOTER -->
  <%- include('partials/footer') %>
</body>
</html>